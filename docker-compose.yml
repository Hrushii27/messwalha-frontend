services:
  backend:
    build: ./backend
    environment:
      JWT_SECRET: ${JWT_SECRET:-your_super_secret_jwt_key}
      PORT: 5000
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
